# AR-40A mechanical trim log
#
# The trimmer sits under the calibration sticker: 1 turn ~ 5e-10, 10 turns end to
# end (docs/hardware.md). Nothing records where it sits, so this file is the only
# record of where it has been put. Fill in a row per move, including the ones that
# turned out to be the wrong way.
#
# Read the offset from ref_freq_offset (chrony drift file, 1e-12 resolution) with a
# fresh "drift file age", never from chronyc tracking, which quantises at 1e-9 and
# will show a flat 0.003 ppm while the rubidium moves by 5e-10. gpsstat prints both
# the offset and the turns still needed to null it.
#
# Wait at least 1 h after each move before believing a reading, and expect creep for
# hours afterwards. Judge a move by the trend over several readings, not the first.
#
# DIRECTION: forward, which is CLOCKWISE, raises frequency. Confirmed by the operator
# 2026-09-20; the two words are used interchangeably below. Established by the half turn,
# which moved +2.44e-10, so sensitivity is 4.88e-10 per turn against the manual's 5e-10.
#
# RANGE IS THE THING TO WATCH. The trimmer has 10 turns end to end and nobody knows
# where it started. Nulling the original -2.5510e-09 takes about 5.2 turns forward in
# total, so if it began near centre the endstop is roughly where the job finishes. A
# move that produces less than ~4.9e-10 per turn is the endstop arriving, not a bad
# reading; stop rather than force it.
#
# date/time (UTC) | move        | offset before | offset after (>=1 h) | notes

2026-09-20 08:00  | (baseline)  | -2.5510e-09   | -                    | 9 days after the 2026-09-11 power cycle, undisturbed. Hourly spread <=5e-12, chrony skew 3.2e-11, drift file 48 min old. 5.10 turns of 10 would null it.
2026-09-20 08:29  | +0.5 fwd    | -2.5510e-09   | -2.3050e-09 (09:43)  | First move, and it settles the direction: FORWARD RAISES FREQUENCY. "Forward" as the operator faces the trimmer; clockwise not independently confirmed. Total from baseline: +0.5 turn. Moved +2.460e-10, i.e. 4.92e-10 per turn against the manual's 5e-10 — spec good to 1.6%. Settled reading: chrony skew back to 3.10e-11 from a post-move peak of 1.45e-10, drift file 10 min old, 1 h 14 min after the turn. (A provisional -2.3070e-09 read at 08:48 while the servo was still chasing; the settled value is 2e-12 away from it, so the servo had very nearly converged already.) Remaining to null: 4.68 turns, 5.18 from baseline.
2026-09-20 09:56  | +4 cw       | -2.3050e-09   | -3.5000e-10 (10:34)  | Second move, done in one go rather than in steps: each poke costs a settling period, and one disturbance to the aging record beats four. Stopped deliberately short of the null — nulling needs 5.18 turns from baseline while a centre-started trimmer has only 5.0, and the offset is cosmetic anyway since chrony carries it. Moved +1.9550e-09, which back-solves to 3.97 turns at the 4.92e-10 established by the first move: the "about 4 turns" was 4.00. NO ENDSTOP: sensitivity over these 4 turns was 4.888e-10 per turn against 4.920e-10 over the first half turn, so the trimmer is still linear and has travel left. Settled fast — skew 2.50e-11 at 10:34, below the 2.5-3.4e-11 pre-trim baseline, 38 min after the move. Total from baseline: 4.47 turns of 10. Remaining to null: 0.70 turns. Holdover 220.1 -> 30.2 us/day.
#
# STATE 2026-09-20: -3.50e-10, down from -2.5510e-09, a factor of 7.3. Left here on
# purpose. Going further buys nothing measurable (chrony carries the offset; the
# Orion and bench gear want ppm) and spends the last of a margin whose size is still
# unknown. Re-read the next morning: that is the reading that would finally show
# mechanical creep, which has not appeared yet — the first move drifted 2e-12 in the
# hour after it, and this one settled below its own starting skew.
