# ZED-F9T fixed position from PPP — 2026-09-12 data, rapid products
#
# Source: NRCan CSRS-PPP 5.15.5, static, ITRF20 (IGc20), GRS80 ellipsoid,
# resubmitted 2026-09-19 against EMR rapid products. Report: ppp-2026-09-12-rapid.sum
# Input: 23.2 h of UBX-RXM-RAWX logged 2026-09-11 10:30 to 2026-09-12 09:42 GPS,
# decimated to 30 s. 2768 of 2783 epochs processed.
#
#   lat 60.179598658   lon 24.958725025   height 22.0279 m ellipsoidal
#   CSRS sigma 0.080 / 0.105 / 0.135 m (1 sigma) -> 0.19 m 3D -> 0.63 ns
#   used here 0.080 / 0.35  / 0.135 m           -> 0.38 m 3D -> 1.28 ns
#
# The east figure is widened on purpose. From 17:00 on the first day to the end
# the forward filter stays within 0.33 m north and 0.23 m up, but wanders 0.69 m
# east (21.10 to 21.79 m from the a-priori), and the final value sits at the edge
# of that range. A float solution that still moves 0.7 m has not earned 0.105 m.
#
# This replaces the ultra-rapid solution of 2026-09-12 (ppp-2026-09-12.sum), which
# sat 1.27 m away: 0.04 m N, 0.97 m W, 0.82 m lower. That is 5.7 of its own east
# sigmas, so its 0.41 m was not honest. The reason is in its EPO line, missed at
# the time: only 410 of 2769 epochs were processed, about 3.4 h of the 23.2 h. This
# run used all of them.
#
# Height is ellipsoidal, not orthometric. CGVD2013 is reported OUTSIDE_GRID_LIMIT,
# this being Finland, so there is no geoid height to confuse it with here.
#
# Still a float solution, and the rapid products did not change that:
#   IAR GPS 0.00%, IAR GAL OFF
#   Galileo is in, but E1 only: CSRS-PPP does not take the F9T's E5b, and the E1
#   code residual is 30 m RMS, so it adds little
#   ANT NOT FOUND; no antenna phase-centre model applied
#
# The limit is the data, not the products. 47% of GPS and 48% of Galileo
# observations carry no L1 carrier phase in the RINEX, all of them weak signals
# (mean 24 dB-Hz against 41 dB-Hz where phase is present), leaving 7655 of 24177
# GPS observations fully dual-frequency. tools/f9t-skymap shows where they go
# missing: the half of the sky from N clockwise to SSE yields 0-2% phase at every
# elevation. That is also why east is the weak axis above — east is the blocked
# side. Final products will not fix it; see docs/hardware.md.
#
# ANT NOT FOUND matters less than it sounds. A timing receiver in TMODE fixed
# computes ranges to the antenna phase centre, so the uncorrected APC position is
# the self-consistent choice here, not a defect.

CFG-TMODE-LAT            601795987
CFG-TMODE-LAT_HP         -42
CFG-TMODE-LON            249587250
CFG-TMODE-LON_HP         25
CFG-TMODE-HEIGHT         2203
CFG-TMODE-HEIGHT_HP      -21
CFG-TMODE-FIXED_POS_ACC  3840
